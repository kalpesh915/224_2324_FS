/**
    read file from disk and convert first character of every
    word in uppercase
*/

#include<stdio.h>

void main(){
    FILE *rfp;
    char ip;

    rfp = fopen("data4.txt", "r");


    fclose(rfp);
}
