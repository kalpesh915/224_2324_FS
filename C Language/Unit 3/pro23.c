/**
    Multi Dimensional Array
*/

#include<stdio.h>
#define size 3

void main(){
    int ip[size][size][size][size][size];

    printf("\n Size of Array is %d Bytes and Array Elements is %d", sizeof(ip), sizeof(ip) / sizeof(int));
}
