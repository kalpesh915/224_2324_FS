/**
    increment operator
*/

#include<stdio.h>

void main(){
    int ip = 10;

    printf("\n value of ip is %d", ip);

    ip++;
    printf("\n value of ip is %d", ip);
    ip++;
    printf("\n value of ip is %d", ip);
    ip++;
    printf("\n value of ip is %d", ip);
    ip++;
    printf("\n value of ip is %d", ip);
    ip++;
    printf("\n value of ip is %d", ip);
}
